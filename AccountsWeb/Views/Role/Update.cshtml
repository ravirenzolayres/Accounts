@using AccountsModel;
@model Role

@{
    ViewBag.Title = "Update";
}

<h2>Update</h2>

<div class="row">
    <div class="col-sm-12">
        <form action="~/User/Update" method="post">
            @{
                Html.RenderPartial("_Role", Model);
            }
        </form>
    </div>
</div>